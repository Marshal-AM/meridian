{
  "phase": "0-devnet",
  "completedAt": null,
  "steps": [
    "Copy .env.example to .env and set DEVNET_CLIENT_SECRET",
    "pnpm install && pnpm build",
    "wsl bash scripts/setup-daml-wsl.sh (Daml via WSL on Windows)",
    "pnpm allocate:devnet",
    "pnpm upload:dar:devnet",
    "powershell -File infra/scripts/verify-devnet.ps1"
  ]
}
